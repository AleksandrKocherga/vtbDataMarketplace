import { createAction } from '@reduxjs/toolkit';

export const addDataset = createAction('ADD_DATASET');
export const addUser = createAction('ADD_USER');
export const addDatasetToUser = createAction('ADD_DATASET_TO_USET');
